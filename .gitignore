node_modules/

# Build output (generated by `npm run build`)
dist/

# Pack compile cache (compiled from packs/<name>/ JSON source, copied into dist/ by Vite)
public/packs/

# One-time pack migration backup (safe to delete after verifying in Foundry)
.pack-ldb-backup/

# Local implementation/refactor notes (per-feature build history; not part of the repo)
dev-notes/

# Claude Code memory (does not belong in the module repo)
memory/
Classes/
node_modules/

# Build output (generated by `npm run build`)
dist/

# Pack compile cache (compiled from packs/<name>/ JSON source, copied into dist/ by Vite)
public/packs/

# One-time pack migration backup (safe to delete after verifying in Foundry)
.pack-ldb-backup/